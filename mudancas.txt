Oq pensamos: 
- criar uma superclasse de objetos parados # abstração
- metodo de construção padrão # coesão

Construtor
- todas as linhas é igual para todos os objetos parados do jogo
- override para o self.rect.y

coesão
- mudar codigo dos gompas para a pasta de personagens

    
def __init__(self, img, l, c):
    # Construtor da classe mãe (Sprite).
    pygame.sprite.Sprite.__init__(self)
    self.image = img
    self.rect = self.image.get_rect()
    self.rect.x = c * tile_size
    self.rect.y = l*tile_size